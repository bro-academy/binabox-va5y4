<section class="team">
	<h2 class="team__title title">{{ team.title }}</h2>
	<ul class="team__list">
		{% for member in team.members %}
			{% include './team-member.twig' with { member: member } %}
		{% endfor %}
	</ul>
</section>
